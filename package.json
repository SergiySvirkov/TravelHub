{
  "name": "travelhub",
  "version": "1.0.0",
  "description": "A web application for seamless travel planning and booking.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "client": "cd client && npm start",
    "server": "cd server && npm start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["travel", "booking", "flights", "hotels", "activities"],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1",
    "mongoose": "^6.0.12",
    // Add other dependencies as needed
  },
  "devDependencies": {
    // Add any development dependencies here
  }
}
